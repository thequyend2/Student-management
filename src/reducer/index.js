import { combineReducers } from "redux";
import studentReducer from "../containers/studentManagementPage/reducer";

const rootReducer = combineReducers({
  studentReducer
});

export default rootReducer;
